{
  "routes": [
    {
      "method": "GET",
      "path": "/university",
      "handler": "university.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/university",
      "handler": "university.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/university",
      "handler": "university.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
